# typegoose reaches into mongoose's internals and @suites resolves DI adapters by
# walking node_modules, both of which assume a flat tree. A hoisted linker keeps
# pnpm's speed without pnpm's strict isolation breaking them.
node-linker=hoisted
strict-peer-dependencies=false
